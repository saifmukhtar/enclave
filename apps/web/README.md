# Enclave React

React + Vite version of the Enclave landing page.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

This app is intentionally separate from the existing static site so the original page stays untouched.