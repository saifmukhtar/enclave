<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera & Microphone — Video calls and kiss gesture overlay -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Haptics — Kiss and heartbeat vibration engine -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Biometrics — Vault unlock and app lock screen -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Audio — WebRTC call audio routing -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Wake Lock — Keep call alive while screen is off -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Notifications — FCM push alerts and kiss invite notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground Services — Screen share capture, music playback, and background sync -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application
        android:allowBackup="false"
        android:label="Enclave"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:windowSoftInputMode="adjustResize"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep Link for Cryptographic Invite Pairing -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="enclave" android:host="invite" />
            </intent-filter>
        </activity>

        <!-- Music playback foreground service (Media3 / ExoPlayer) -->
        <service
            android:name=".media.MusicPlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService"/>
            </intent-filter>
        </service>

        <!-- Screen share foreground service (WebRTC MediaProjection) -->
        <service
            android:name=".webrtc.ScreenShareService"
            android:foregroundServiceType="mediaProjection"
            android:exported="false" />

        <!-- Ntfy Native WebSocket Listener -->
        <service
            android:name=".notifications.NtfyListenerService"
            android:foregroundServiceType="remoteMessaging"
            android:exported="false" />

        <!-- Companion Status Widget -->
        <receiver
            android:name=".ui.widget.CompanionWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/companion_widget_info" />
        </receiver>

        <!-- WorkManager foreground service for data sync workers -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- FileProvider for secure E2EE document sharing and opening -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.enclave.app.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
